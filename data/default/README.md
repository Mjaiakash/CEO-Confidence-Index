# Default document

The application is configured to use the bundled `annual-report-2025-2026.pdf` as its default document.

Users can upload other PDF annual reports from the Streamlit sidebar at any time.
